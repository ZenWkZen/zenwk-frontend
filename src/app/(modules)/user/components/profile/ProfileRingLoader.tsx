import React from 'react';
import { RingLoader } from 'react-spinners';

const ProfileRingLoader = () => {
    return <RingLoader color="#155E75" size={17} speedMultiplier={1.5} />;
};

export default ProfileRingLoader;
